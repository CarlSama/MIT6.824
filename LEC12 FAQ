Q: 当前使用FaRM的系统？
A: 只有FaRM。还未在生产环境中使用，还是个很新的研究原型。我认为它会影响到未来的设计，后者会被发展为产品。

Q: RDMA和RPC的区别？什么是单向RDMA？
A: RDMA是现代NIC（网络接口卡）的特性。NIC等待从网络上到来的特殊指令包，并且执行这些指令（不需要将包转发给CPU）。
这些指令表示读写内存的操作，并且将结果从网络上返回。此外，RDMA NIC允许程序代码直接与NIC硬件交互，发送特殊的RDMA命令包，
在“硬件ACK”包（表明对端NIC执行了命令）到达时收到提醒。
